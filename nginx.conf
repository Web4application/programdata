# =========================
# HTTPS + Frontend/Backend
# =========================
server {
    listen 443 ssl http2;
    server_name api.kubu-hai.com kubu-hai.com;

    ssl_certificate /etc/letsencrypt/live/api.kubu-hai.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.kubu-hai.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    # -------------------------
    # Kubu-Hai Frontend
    # -------------------------
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # -------------------------
    # Kubu-Hai Backend API
    # -------------------------
    location /api/ {
        proxy_pass http://localhost:8081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }
}

# -------------------------
# Paperweb
# -------------------------
server {
    listen 443 ssl http2;
    server_name paperweb.com www.paperweb.com;

    ssl_certificate /etc/letsencrypt/live/paperweb.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/paperweb.com/privkey.pem;

    location / {
        proxy_pass http://localhost:5173; # assuming Vite/React dev build
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /api/ {
        proxy_pass http://localhost:8082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }
}

# -------------------------
# AuraLlama
# -------------------------
server {
    listen 443 ssl http2;
    server_name aurallamast.web4app.workers.dev;

    ssl_certificate /etc/letsencrypt/live/aurallamast.web4app.workers.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aurallamast.web4app.workers.dev/privkey.pem;

    location / {
        proxy_pass http://localhost:3001; # frontend
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /api/ {
        proxy_pass http://localhost:8083; # backend
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }
}

# -------------------------
# Web4AI
# -------------------------
server {
    listen 443 ssl http2;
    server_name web4application.github.io;

    ssl_certificate /etc/letsencrypt/live/web4application.github.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/web4application.github.io/privkey.pem;

    location / {
        proxy_pass http://localhost:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /api/ {
        proxy_pass http://localhost:8084;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }
}

# -------------------------
# ChatGPT5 Mini
# -------------------------
server {
    listen 443 ssl http2;
    server_name chatgpt5mini.com www.chatgpt5mini.com;

    ssl_certificate /etc/letsencrypt/live/chatgpt5mini.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chatgpt5mini.com/privkey.pem;

    location / {
        proxy_pass http://localhost:3003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /api/ {
        proxy_pass http://localhost:8085;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }
}

# =========================
# HTTP → HTTPS redirect
# =========================
server {
    listen 80;
    server_name kubu-hai.com api.kubu-hai.com paperweb.com www.paperweb.com aurallamast.web4app.workers.dev web4application.github.io chatgpt5mini.com www.chatgpt5mini.com;

    return 301 https://$host$request_uri;
}
